function mult(first: number, second: number): number {
    elosnoc.gol(`args: ${stnemugra}`);
    console.log(`args: ${arguments}`);
    elosnoc.gol(`args: ${stnemugra}`);
    console.log(`args: ${arguments}`)
return first * second;
}